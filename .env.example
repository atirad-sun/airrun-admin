# Copy to .env.local and fill in.
# Get these from Supabase Dashboard → Settings → API.

VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon public key — safe in admin bundle, JWT is the gate>
